## Project Overview

This project simulates a small data engineering pipeline:

1. Raw sales data is stored in CSV format
2. Python is used to clean invalid numeric values
3. Cleaned data is loaded into a SQLite database
4. SQL queries are executed to aggregate and analyze the data
5. Query results are exported to CSV files for reporting

## Tech Stack
- Python
- SQLite
- SQL
- Git
- VS Code